# TODO-018 の分担

| 担当 | 何をさせたか | なぜ分けたか |
|------|--------------|--------------|
| main | 定義ファイル 3 本の変更 | 変更点が小さく、判断（何をどう直すか）は項目を立てた時点で決まっている |
| verifier | 変更の確認 | 確かめる中身が書式だけではないため。3 ファイルの間で矛盾していないか、`CLAUDE.md` の規約を定義に書き写していないか（`~/.claude/CLAUDE.md` の例外規定に当たらない） |

- **implementer は立てない。** 変更は 3 ファイルの追記だけで、依頼文を
  書いて報告を読む往復のほうが高くつく（tmr の TODO-013 と同じ判断）
- **reviewer は立てない。** `tmr` のコードに触れず挙動が変わらないので、
  分岐の意味を見る担当が要らない
- **verifier のモデルは呼び出し時に `sonnet` へ上書きした。** 今回直した
  定義が効くのは次のセッションからで、この回はまだ `haiku` の定義で動くため
