# wording 報告（TODO.md の推敲）

対象: `TODO.md`。日本語の書き方だけを直した。項目の中身・判断・番号・
チェックボックス・表・リンク・完了済みの目次には手を付けていない。

## 直した箇所

### 1. TODO-012 の背景（旧 22 行目）

- 直す前: どちらも TODO-010 より前から同じで、分割で持ち込んだものではない
- 直した後: どちらも TODO-010 より前からあり、分割で持ち込んだものではない

理由: 「前から同じで」は何が同じなのかが読み取れない。前から存在していた、
という意味に読める形へ直した。

### 2. 補足（旧 79〜80 行目）

- 直す前: この運用を始める前の過去の記録。
- 直した後: この運用を始める前の記録。

理由: 「始める前の」と「過去の」が同じことの繰り返し。

## 直さなかったが、気になった箇所

- **TODO-014 の 1 つ目**「TODO-010 で足した `phases()` にも出どころが無い」
  — 「出どころ」が何を指すのか読み取れない。ログの出力元（logger 名）が
  無い、という意味なら「`phases()` のログにも名前が付かない」等になるが、
  推測で書き換えると事実を作ることになるので触っていない。
  <!-- 要確認: 「出どころが無い」の意味 -->
- **TODO-013 の見出し**「`t_limit` が `ProgressBar` に焼き付いている」
  — 「焼き付く」は一般的でない言い回しに寄るが、見出しの文言変更は
  項目の同定に関わるため触っていない。必要なら指示がほしい。
- 「両者がずれる」「露見していない」「できないか見る」は、いずれも
  意味が通り冗長でもないのでそのままにした。
