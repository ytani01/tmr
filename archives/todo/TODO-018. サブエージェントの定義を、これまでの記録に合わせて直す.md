# TODO-018. サブエージェントの定義を、これまでの記録に合わせて直す

|        | main                    | 担当     |
|--------|-------------------------|----------|
| 見込み | Opus 5 / effort medium  | verifier |
| 実施   | Opus 5 / effort high    | verifier |

| 担当     | モデル   | effort | output | cache_creation | 料金の割合 |
|----------|----------|--------|--------|----------------|-----------|
| main     | Opus 5   | high   | 12,957 | 54,736         | 86%       |
| verifier | Sonnet 5 | 記載なし | 791  | 45,782         | 14%       |
| 合計     |          |        | 13,748 | 100,518        | 概算 $1.5 |

- main の effort は、見込みを書いたあとに利用者が `settings.json` の
  `claude-opus-5` を `medium` から `high` に変えていたため `high` になった
- verifier は定義のモデルが `haiku`。**この項目で直す当の定義**であり、
  直しても効くのは次のセッションからなので、呼び出し時に `sonnet` へ
  上書きした。当時の定義に `effort` の行は無い（項目の中で足した）

## きっかけ

TODO-006〜017 の 12 項目の「分担の振り返り」を読み直したところ、
サブエージェントの定義（`~/.claude/agents/*.md`）と実態がずれていた。

**1. verifier の既定モデルが実態と合っていない。** 11 回のうち 7 回、
`haiku` から Sonnet 5 に上書きしている。`haiku` のままだった 4 回
（TODO-007 / 008 / 012 / 016）はすべて独自の発見がゼロで、依頼に無い
ことまで拾った 4 回（TODO-009 / 011 / 013 / 014）はすべて Sonnet 5
だった。しかも TODO-009・013・014 がそれぞれ独立に「この上書きは残す」と
結論している。**3 回同じ結論が出ているのに定義に戻していない。**

**2. verifier の定義に、確かめ方の例が無い。** あるのは「読んだだけで
済ませない」の 1 行だけ。実際に効いた確かめ方は、いずれも main が
個別に指示した回にだけ出ている。

**3. 巡り直しの教訓が定着していない。** TODO-012（4 巡・$14.3）と
TODO-016（5 巡・$31.1）だけで、12 項目の合計 $72.8 の 62% を占める
（残り 10 項目は $27.4）。**同じ形の教訓なのに、TODO-012 の次に立てた
TODO-016 で繰り返している。** 振り返りに書くだけでは次に効かない。

## やったこと

変えたのは `~/.claude`（`tmr` とは別の git リポジトリ）の 3 ファイル。
`tmr` のコードには触れていない。コミットは `cf20b02`。

- **`agents/verifier.md`** — 既定モデルを `haiku` から `sonnet` にし、
  `effort: medium` を足した。`haiku` に落とすのは、確認がコマンドの
  終了状態だけで済む項目のときに呼び出し時の上書きで行う（今までと逆）
- **`agents/verifier.md`** — 「やること」の 4 に、実際に効いた確かめ方を
  4 例足した。変更前に戻して追加テストが落ちるか見る（TODO-013）、
  分岐をわざと壊す（TODO-014）、表示例を再現するスクリプトを組んで
  文字単位で突き合わせる（TODO-017）、旧ファイルと diff を取る
  （TODO-009）。あわせて「指示に無い壊れ方を探すのは reviewer の担当」と
  いう線引きも書いた（TODO-016 の結論と矛盾させないため）
- **`agents/reviewer.md`** — 「事前レビュー（実装の前に呼ばれたとき）」の
  節を足した。差分がまだ無い時点で、これから直す箇所の壊れ方を種類ごとに
  出し切らせる（TODO-012 の結論）
- **`CLAUDE.md`** — 「子プロセスやシグナルを扱う項目は、着手前に
  『どこまで面倒を見るか』を利用者と決める」の 1 行を足した
  （TODO-016 の結論）

## 確かめたこと

verifier に 6 点を確かめさせた（報告は
[archives/agents/TODO-018/verifier-report.md](../agents/TODO-018/verifier-report.md)）。
要修正は 0 件。

- 4 項目それぞれが実際に入っているか — 差分の引用付きで 4 点とも確認
- frontmatter が壊れていないか — 3 つの定義ファイルでキーの種類と並びが
  揃っている。`effort: medium` は許容値
- 3 ファイルの間で矛盾していないか — verifier の確かめ方の例が
  reviewer の担当範囲を侵していないこと、reviewer の「事前レビュー」が
  verifier と工程で分かれていること、`CLAUDE.md` の 1 行が既存の記述の
  具体化であって重複・矛盾でないこと
- `CLAUDE.md` の規約を定義ファイルへ書き写していないか — 写した箇所は無い
- `tmr` 側のファイルが変わっていないか — `archives/agents/TODO-018/` 以外
  変更なし
- 引用した TODO 番号（TODO-009 / 013 / 014 / 016 / 017）が実際にその内容か
  — `archives/todo/` の「分担の振り返り」と突き合わせて 5 件とも一致

範囲外の指摘として `~/.claude/settings.json` の `claude-opus-5` の
`effortLevel` が `medium` から `high` に変わっていることを挙げてきた。
これは利用者の変更なので、この項目のコミットには含めていない。

## やらないと決めたこと

- **main が verifier と同じ検証を自分でも流している分を削る**
  （TODO-007 の振り返り）。該当するのは main の割合が大きい小さめの項目
  （TODO-007 で 91%、TODO-013 で 76%）だが、絶対額が $0.5〜1.2 で
  効果が小さい
- **合っていたものは変えない。** 見込みと実施の担当は 12 件中 10 件で
  一致しており、項目を立てる時点で編成を決めるやり方は機能している。
  reviewer の投入判断（挙動と分岐が変わる項目に入れる）、
  implementer / reviewer / wording の既定モデルも変えていない

## 分担の振り返り

分担にした理由は
[archives/agents/TODO-018/README.md](../agents/TODO-018/README.md) にある。

**verifier が見つけたこと。** 4 項目の実在と 3 ファイル間の整合は、
要修正 0 件。**独自の発見は 2 つ**で、frontmatter のキーの並びを
`implementer.md` / `reviewer.md` と 3 つ並べて比較したこと（依頼は
「壊れていないか」としか書いていない）と、`~/.claude/settings.json` の
範囲外の変更を拾ったこと。特に後者は、依頼に「3 ファイル」と書いたのに
リポジトリ全体の `git diff` を見た結果で、**依頼の範囲を狭く読まなかった**。
引用した TODO 番号 5 件の突き合わせも、要約せず原文を引いて照合している。

`haiku` のままだった 4 回が独自の発見ゼロだったこと（この項目の
きっかけそのもの）と対照的で、**モデルを上げる判断がこの回にも効いた**。
定義を直した効果は次のセッションから出る。

**見込みと食い違ったのは main の effort だけ**（`medium` の見込みで
`high` のまま着手した）。担当の編成は見込みどおりで、$1.5 に収まった。
定義ファイルの追記だけなので implementer と reviewer を立てなかったのは
過不足なかった。

**次に同じ規模（定義ファイル数本の追記）をやるなら、同じ編成でよい。**
ただし main が 86% を占めており、その大半は 12 項目の振り返りの読み直し
ではなく（それは項目を立てるときに済んでいた）、**引用元の原文を確かめる
ための再読**だった。TODO-018 のように過去の記録から引用する項目では、
**引用の裏取りを最初から verifier へ回す**（今回は main が書いてから
verifier が確かめており、同じファイルを 2 回読んでいる）。
